__pycache__/
*.pyc

# Landscape/terrain raster data — multi-GB TIFFs, fetched from R2 at
# runtime (see ensure_landscape() in src/fogos_triage/landscape.py), not
# meant to live in git.
data/landscape/
data/landscape_pt/
data/AltoMinho/

node_modules/
frontend/dist/

# Cache da série ERA5 usada por scripts/ajusta_lfmc_climatologia.py
# (regenerável: o script rebusca do arquivo Open-Meteo se faltar)
LFM/.meteo_cache_*.json
